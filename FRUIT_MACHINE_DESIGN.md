# 水果老虎机：跑灯版（Fruit Tiger）

这版以用户提供的视频为原型：不是三列独立滚轴，而是一个围绕中央奖盘的 **24 格跑灯**。玩家给 8 种水果分别下注，按下开始后，链上随机数决定跑灯停点，命中哪一类就按该类下注和赔率结算。

## 一局怎么玩

1. 玩家在底部 8 个奖级按钮上调整各自的下注额；初始全部为 `00`，只有点击后才产生下注。按下按钮立即增加 1 点，持续按住约 350ms 后每 90ms 连续增加 1 点，直到该路达到 99；松开、取消、失焦或页面隐藏时停止。普通快速点击每次增加 1 点，不提供双击减注。下注区的「清除全部下注」按钮会将八路下注一键归零，不影响财富或奖金。
2. 顶部 `财富` 显示当前余额，8 个三位数字窗显示 8 路下注金额。
3. 点击「开始」时，若奖金大于 0，先把全部奖金转入财富，本次不下注、不跑灯；奖金为 0 时，才扣除本局总下注，跑灯沿轨道顺时针移动。
4. VRF 返回后，跑灯停在一个轨道格，命中的格子高亮。
5. 命中普通奖级时，玩家收到该格对应的 `下注额 × 倍率`。
6. 命中 `Lucky` 时，进入 Lucky 分支：`吃灯` 不产生中奖格；`幸运送灯` 随机送 1–3 格；`仙女散花` 送 5 格；`财神赐福` 送 8 格。每个送灯格都按对应水果下注额和格子倍率结算。
7. `1–7 小 / 8–14 大` 是视频里的第二种玩法入口；第一版 UI 保留按钮，但先不和跑灯 session 混合，避免赔率和风控耦合。后续可独立做成 `mode=big-small`。

每次旋转是一个 Casino session。自动模式只是串行提交多个独立 session，上一局结算并完成展示后才提交下一局。

## 视频中的轨道布局

轨道按顺时针编号 `0..23`，从顶部左侧开始：

```text
顶部：  橙  铃  BAR×50  BAR  苹果  苹果×3  柠檬
右侧：  西瓜  西瓜×3  Lucky  苹果  橙子×3
底部：  橙  铃  77×3  77  苹果  柠檬×3  柠檬
左侧：  星星  星星×3  Lucky  苹果  铃×3
```

轨道格分成 8 个下注类别：`BAR / 77 / 星星 / 西瓜 / 铃铛 / 柠檬 / 橙子 / 苹果`。同一种类别的多个轨道格共用一路下注。普通格与 `×3` 格是不同的结果倍率；视频中的 `BAR×50` 是视觉上的特殊格，正式合约参数必须单独定义并计入最高赔付风险。

## 第一版赔率和下注模型

底部奖级栏沿用视频顺序：

| 下注类别 | 基础显示倍率 |
| --- | ---: |
| BAR | 120x |
| 77 | 40x |
| 星星 | 30x |
| 西瓜 | 20x |
| 铃铛 | 20x |
| 柠檬 | 15x |
| 橙子 | 10x |
| 苹果 | 5x |

视频里的 `×3` 和 `BAR×50` 必须作为独立 outcome 配置，不应在前端把它们当作装饰后再自行加倍。建议合约使用一张显式的 24 格 outcome 表：每格包含 `symbolId`、`multiplierBps`、`isLucky`。`quoteCaps` 取所有 24 格可能赔付中的最大值；不能只用底部基础奖级估算。

第一版演示规则：

- 普通格：`payout = stake[symbolId] × multiplierBps / 10_000`。
- `×3` 格：`multiplierBps = baseMultiplier × 3`，是独立 outcome。
- `BAR×50`：作为独立的高风险 outcome；当前 UI demo 按画面暂时显示/演示 ×50，正式合约必须在准备金确认后再开启。
- Lucky：按公开经典变体做演示分支：吃灯不中奖；幸运送灯 1–3 格；仙女散花 5 格；财神赐福 8 格。视频和目标商店页没有公开这些分支的精确概率，不能把网络上其他版本的左右 Lucky、×60 或火车 5 灯直接当作本机规则。
- 没有任何一路下注且奖金为 0 时，开始按钮禁用；有奖金时仍可点击「开始」收奖。

合约最终的 RTP 取决于 **24 格权重 × 每路下注分布**，不是单独看底部显示的倍率。上线前必须固定每一格的权重、最高倍率、最坏下注组合和目标 house edge，再生成 `quoteRiskParams`。

公开资料没有找到与用户视频完全对应的逐格概率表；商店页只说明下注、跑灯、倍率和特殊节目，通用规则页也没有给出这台机器的可验证权重。因此原型现在使用一张**94% RTP 保守演示权重表**，总权重为 100,000，目的是降低原来均匀 `1/24` 抽样造成的高中奖频率，不声称是原机官方概率。正式上线前必须把它替换为审计确认的概率表。

当前演示格权重（按轨道 0–23 顺序）为：

```text
[7500, 1666, 500, 20, 6250, 6500, 2900, 1667, 6500, 240, 6250, 6500,
 7500, 1667, 6500, 1000, 6250, 6500, 2900, 3000, 6500, 240, 6250, 5200]
```

因此每格概率是 `cellWeight / 100000`。例如 BAR×120 格为 `20 / 100000 = 0.02%`，BAR×50 格为 `500 / 100000 = 0.50%`，普通苹果格各为 `6250 / 100000 = 6.25%`，苹果×3 格为 `6500 / 100000 = 6.50%`。按每个下注档各下注 1 点、Lucky 不计额外返奖计算，基础理论 RTP 为 **94%**；×3 格的赔率仍是视频验证的“该路下注 ×3”，不是底部基础赔率再乘三。

## SDK 映射

### `gameData`

```text
uint8 schemaVersion
uint8 mode                 // 0 = ring slot, 1 = future big-small
uint16 paytableVersion
uint256[8] stakes           // 8 路下注，token base units
```

实际实现时建议传下注档位索引，避免 `gameData` 过长；合约按档位表恢复 token 金额。下注档位、最大总下注和每路上限必须由合约验证。

### `gameState`

```text
uint8 stateVersion
uint8 outcomeIndex          // 0..23
uint8 symbolId              // 0..7
uint16 multiplierBps
uint8 bonusType              // 0 normal, 1 吃灯, 2 送灯, 3 仙女散花, 4 财神赐福
uint8[] bonusOutcomeIndices  // Lucky 多格结算结果
bytes32 providerRandomness   // 审计展示，不作为新熵
```

### `ICasinoGameV2`

- `quoteCaps`：`maxEscrowStake = totalStake`；`maxReservedProfit` 使用所有可达 outcome 的最大赔付减去总下注，取不小于 0 的值。
- `quoteRiskParams`：返回真实的最高总赔付、最高 outcome 的边际概率、完整下注模型的期望赔付和重尾方差。
- `onSessionStart`：校验下注，提交完整最大准备金，进入 `WAITING_RANDOMNESS`。
- `onRandomness`：用同一套 outcome table 选择轨道格；普通格直接 `SETTLED`，Lucky 在同一回调中按已配置分支从同一 VRF 字节流继续抽取并 `SETTLED`。
- `onPlayerAction`：第一版不使用。
- `quoteForfeitPayout`：返回 `0`。跑灯结果在 VRF 完成前不可兑现，不能给出会被择时利用的现金报价。

公开的同类商店页确认常见“中奖后猜大小，猜中翻倍”；本原型把 `1–7 小 / 8–14 大` 接到奖金区，猜中把待领奖金变为 2 倍，猜错清零。这仍是 demo 规则，正式合约要把风险和最高赔付一起报价。

抽样必须无偏。不能直接 `uint256(randomness) % 24` 或 `% totalWeight`；采用拒绝采样后再映射到轨道或累计权重。客户端只镜像展示，链上 payout 才是权威。

## UI 结构

### 顶部

- 紫色渐变背景和金色机台框。
- 返回、设置、帮助、分享、礼盒按钮。
- 左侧“奖金”计数窗，右侧“财富”计数窗。

### 中央跑灯机台

- 外圈 24 个带金色边框的图标格。
- 中央粉紫玻璃牌写「水果老虎机」。
- 中央数字窗显示当前倍率和本局中奖额。
- 跑灯格用金色呼吸边框逐格移动，停止后保留 1.4 秒高亮。

### 底部控制

- 左蓝色箭头：把财富转入奖金用于猜大小，转入额度永远按当前奖金的 50% 计算（向上取整）；奖金为 0 时转入 1，且不超过当前财富余额。例如奖金为 20 时，左移增加 10，奖金变为 30。
- 右蓝色箭头：把奖金转回财富用于收分，转出额度永远按当前奖金的 50% 计算（向上取整）；奖金为 0 时不转。例如起点奖金为 20 时，右移转回 10，奖金变为 10。
- 箭头转账按比例一次完成，不再按 1 点逐点移动；中奖后奖金仍停留在左侧，也可点击「开始」一次收回全部奖金。
- 绿色 `1–7 小` 和 `8–14 大`：首版显示为未启用的 future mode。
- 红色「开始」：有奖金时播放全部奖金转入财富的收奖效果，本次不开始新一局；奖金为 0 时提交本局 session。
- 紫色「正常」：切换正常/快速动画。
- 8 个三位数下注窗：显示每路 stake。
- 8 个奖级按钮：按下立即增加 1 点，按住约 350ms 后每 90ms 连续加注，直到单路 99；松开、取消、失焦或页面隐藏时停止。普通快速点击逐次增加 1 点，不提供双击减注。下注区提供「清除全部下注」按钮，一键归零八路下注，不影响财富或奖金；跑灯、收奖和 Lucky 忙碌期间锁定下注操作。

### 链上状态反馈

- `READY`：允许下注。
- `WAITING_RANDOMNESS`：跑灯持续移动，不能重复提交。
- `SETTLED`：读取最终 `gameState`，停止跑灯并播放中奖效果。
- `CANCELLED`：随机数超时，显示取消并允许重新开始。
- 结果动画结束后调用 `revealOutcome`，否则 Host 余额显示不会释放赢款。

## 第二阶段

- 真正实现 `1–7 小 / 8–14 大` 独立玩法和其概率表。
- Lucky 触发免费重抽/奖励池，但必须先重新计算 session 的最大赔付和准备金。
- BAR×50 和共享 Jackpot 需要单独的 heavy-tail 风控设计。
- 读取 `raw.randomnessRequests`，显示每个 VRF request 的公平性验证结果。

## 音效

原型使用 `audio.js` 的原创 WebAudio 合成，不抓取视频或第三方机台录音。音色按视频频谱和街机听感拆成：下注短音、跑灯 tick、减速/停止、普通中奖、大奖、Lucky 分支、多格送灯、奖金/财富转移和猜大小成功/失败。首次点击开始或下注时解锁 AudioContext；静音和音量保存在浏览器本地。
